---
import LivePriceBox from "./LivePriceBox.astro";

// Define props from Astro
const { title, subTitle } = Astro.props;

// Define TypeScript interface for props
interface Props {
  title: string;
  subTitle?: string;
}
---

<section
  class="mx-auto max-w-[85rem] px-4 py-4 sm:px-6 lg:px-8 lg:py-14 2xl:max-w-full"
>
  <!-- Title and description -->
  <div class="mx-auto mb-6 w-full space-y-1 text-center sm:w-1/2 lg:w-1/3">
    <h2
      class="text-balance text-2xl font-bold leading-tight text-neutral-800 dark:text-neutral-200 sm:text-3xl"
    >
      {title}
    </h2>
    {
      subTitle && (
        <p class="text-pretty leading-tight text-neutral-600 dark:text-neutral-400">
          {subTitle}
        </p>
      )
    }
  </div>
  <div class="grid grid-cols-1 gap-3 sm:grid-cols-2 md:grid-cols-4 lg:mx-auto">
    <!-- Clients Group SVGs -->
    <!-- First -->
    <div class="">
      <LivePriceBox
        title="قیمت سکه"
        price={4.0}
        unit="میلیون تومان"
        delta={-1}
      />
    </div>
    <!-- Second -->
    <div class="">
      <LivePriceBox
        title="طلای آب شده"
        price={3.555}
        unit="میلیون تومان / گرم"
        delta={1}
      />
    </div>
    <!-- Third -->
    <div class="">
      <LivePriceBox
        title="طلای جهانی"
        price={3.423}
        unit="میلیون تومان / گرم"
      />
    </div>
    <!-- Fourth -->
    <div class="">
      <LivePriceBox
        title="طلای ۲۴ عیار"
        price={3.852}
        unit="میلیون تومان / گرم"
        delta={1}
      />
    </div>
  </div>
</section>
